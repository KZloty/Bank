@model IEnumerable<Bank.Entities.Telefon>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Klient.Imie)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NumerTelefonu)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Klient.Imie)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NumerTelefonu)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TelefonID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TelefonID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TelefonID })
        </td>
    </tr>
}

</table>
